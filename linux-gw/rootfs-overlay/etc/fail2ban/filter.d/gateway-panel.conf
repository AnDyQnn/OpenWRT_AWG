# Фильтр fail2ban для веб-панели Gateway Hub.
# Ловит строки неудачных входов из /var/log/panel-auth.log:
#   2026-06-26 18:00:00 panel-auth: FAILED login from 1.2.3.4 user=admin
[Definition]
failregex = ^.*panel-auth: FAILED login from <HOST> user=.*$
ignoreregex =
